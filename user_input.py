#input is an inbuilt function used to accept user input in the terminal

name = input("What is your name: ") #we are storing the user input in the variable name

print("Welcome " + name + " to Fasam Holdings") # the plus(+) sign is used to combine strings with variable > string concatenation

